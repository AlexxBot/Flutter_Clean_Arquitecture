class Auditoria {
  String userNew = '';
  DateTime dateNew = DateTime.now();
  String userEdit = '';
  DateTime dateEdit = DateTime.now();

  Auditoria(this.userNew, this.dateNew, this.userEdit, this.dateEdit);
}
